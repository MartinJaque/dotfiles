## For install this config
1. Install the extensions:
  - Pop-shell
  - Just Perfection
  - Space Bar
2. Load configs:
```bash
dconf load /org/gnome/shell/extensions/ < extensiones.conf
```
3. Disable ubuntu dock
